import React from 'react';

const Game = () => (
  <div style={styles.container}>
    <h1>Game Screen</h1>
    {/* Game content will be added here */}
  </div>
);

const styles = {
  container: {
    textAlign: 'center',
    padding: '20px',
  },
};

export default Game;