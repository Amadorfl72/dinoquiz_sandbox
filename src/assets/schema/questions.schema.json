{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Question",
  "description": "A question for the DinoQuiz app",
  "type": "object",
  "properties": {
    "statement": {
      "description": "The question statement",
      "type": "string"
    },
    "options": {
      "description": "The answer options",
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 3,
      "maxItems": 3
    },
    "correctIndex": {
      "description": "The index of the correct answer in the options array",
      "type": "integer",
      "minimum": 0
    },
    "dinoId": {
      "description": "The identifier for the dinosaur related to the question",
      "type": "string"
    },
    "funFact": {
      "description": "A fun fact about the dinosaur",
      "type": "string"
    },
    "image": {
      "description": "The filename of the dinosaur image",
      "type": "string"
    }
  },
  "required": ["statement", "options", "correctIndex", "dinoId", "funFact", "image"]
}